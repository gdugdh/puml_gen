@startuml
title POST /api/create_steam_order - Создание заказа на Steam пополнение в DonateHub
start

:Params: **customId**;

:Build request\nwith `custom_id` = **customId**;
note: A1.R1

#palegreen :POST /api/create_steam_order;

:**Response**;
note: A1.R2

if (**Response.Code** = HTTP 200?) then (+)
    :**Response.Body**;
    stop
endif

if (**Response.Code** = HTTP 401?) then (+)
    :HTTP 401 Unauthorized;
    end
endif

:HTTP 400 BadRequest;
end

@enduml